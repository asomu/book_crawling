# Legacy Implementation

이 폴더는 스크립트 기반 v1 구현을 보관한다.

- `src/` 기존 Python 스크립트
- `notes/` ISBN 목록, 메모, 변경 이력
- `build/` PyInstaller 관련 파일과 이전 실행 자산
- `docs/` 과거 분석 문서

새 운영 기준 구현은 루트의 `app/` 및 `docs/`를 따른다.
